# Data

This directory is reserved for schemas, tiny reproducible samples and dataset documentation.

Large/raw/processed datasets are intentionally excluded from Git. Real sources must be profiled before the project fixes spatial units, temporal units, target variables or model inputs.
