# Pipelines

Planned reproducible stages:

```text
source -> profiling -> ingest -> clean -> geospatial -> temporal -> model-ready dataset
```

The first implemented pipeline capability should be profiling, because it determines what spatial and temporal representations are actually supported by the data.
